extends Node3D

# Sets up the fixed isometric camera on _ready().
# No runtime changes needed; the camera is fully configured in the scene.

func _ready() -> void:
	pass  # Camera is positioned via scene properties.
